apiVersion: v1
kind: Service
metadata:
  name: {{ include "aplikacja1.fullname" . }}
  labels:
    {{- include "aplikacja1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aplikacja1.selectorLabels" . | nindent 4 }}
